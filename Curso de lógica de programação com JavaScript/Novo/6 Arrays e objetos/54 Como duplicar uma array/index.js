let numeros = [1, 2, 3, 4, 5]
let numerosDuplicados = []

for (let i = 0; i < numeros.length; i++) {
    numerosDuplicados.push(numeros[i] * 2) // método push adiciona um novo elemento no final da array
}

console.log(numerosDuplicados)