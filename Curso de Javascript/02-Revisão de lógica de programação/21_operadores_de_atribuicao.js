let n = 20 // operador = 

n += 15 // é o mesmo que n = n + 15
n -= 15 // é o mesmo que n = n - 15
n *= 15 // é o mesmo que n = n * 15
n /= 15 // é o mesmo que n = n / 15
n %= 15 // é o mesmo que n = n % 15
n **= 15 // é o mesmo que n = n ** 15